import React from 'react';
import ProgressList from './ProgressList'

export default {
    title: 'Progress List',
    component: ProgressList,
    argTypes: {

    },
};

const Template = (args) => <ProgressList />
export const ProgressListBar = Template.bind({});